# Monitora UTI Frontend

Frontend built with Vite + React + TypeScript + Tailwind.

Install and run:

```
npm install
npm run dev
```

API url: by default `http://localhost:5000` — you can set `VITE_API_BASE`.
